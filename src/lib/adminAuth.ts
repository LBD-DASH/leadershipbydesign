export const ADMIN_AUTH_KEY = 'admin_authenticated';

// NOTE: This is used only for lightweight admin gating in the UI.
// It is intentionally centralized so client + backend function can share the same token.
export const MASTER_EMAIL = 'kevin@kevinbritz.com';
export const MASTER_TOKEN = 'Bypass2024';
